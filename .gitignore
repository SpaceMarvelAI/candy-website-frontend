# dependencies
node_modules

# build output
dist
dist-ssr

# test coverage output
coverage

# predeploy-check temp results
.predeploy

# audit doc (local only)
FRONTEND_AUDIT.md

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# env
.env
.env.local
.env.*.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Claude Code local config
.claude/

# Claude Code project instructions (local only)
CLAUDE.md
